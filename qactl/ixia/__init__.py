"""Consolidated Ixia / IxNetwork support (was the top-level ixia* packages).

Sub-packages:
  qactl.ixia.client  — low-level IxNetwork client (was ``ixia``)
  qactl.ixia.core    — envelope / session cache / request log (was ``ixia_core``)
  qactl.ixia.tools   — high-level tool ops (was ``ixia_tools``)
  qactl.ixia.ctl     — the argparse CLI front (was ``ixiactl``)
"""
