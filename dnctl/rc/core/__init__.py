"""RESTCONF MCP — cross-cutting infrastructure.

Mirrors `dnctl.gnmi.core/` and `netconf-mcp/dnctl.nc.core/`: envelope, request log,
HTTP session helpers, URI builder, ODL mount management, device registry
loader.
"""
